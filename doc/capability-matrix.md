# Capability Matrix

This matrix classifies the current public surface of `zig/` by support level.

| Capability | Level | Phase | Status | Notes |
| --- | --- | --- | --- | --- |
| `HarnessBuilder` configuration capture | Stable Core | 0 | Available | Collects URL, HTML, local/session storage, a `Math.random()` seed, and open/close/print/scroll bootstrap failure seeds before ownership is transferred into `Session`. |
| `Harness` constructors | Stable Core | 0 | Available | `fromHtml`, `fromHtmlWithUrl`, `fromHtmlWithLocalStorage`, `fromHtmlWithUrlAndLocalStorage`, `fromHtmlWithSessionStorage`, and `fromHtmlWithUrlAndSessionStorage` are available. |
| `Harness` accessors | Stable Core | 0 | Available | `url()`, `html()`, and `localStorage()` expose the copied session snapshot. |
| `StorageSeed` | Stable Core | 0 | Available | Represents deterministic local- and session-storage seed pairs. |
| `Error` and `Result(T)` | Stable Core | 0 | Available | `error.InvalidUrl`, `error.InvalidSelector`, `error.AssertionFailed`, `error.DomError`, `error.EventError`, `error.ScriptParse`, `error.ScriptRuntime`, `error.MockError`, `error.TimerError`, and `error.OutOfMemory` cover the public surface; `error.HtmlParse` remains for malformed HTML. |
| `Session` | Internal Only | 0 | Available | Internal copied state with an arena-owned lifetime. It now also owns the internal DOM store, script runtime state, event listener registry, focus/target selector-state snapshots, queued microtasks, fake clock state, and the mock registry. |
| HTML parsing and DOM tree construction | Internal Only | 1 | Available | `src/dom.zig` parses HTML into `DomStore` and rejects malformed markup explicitly. |
| Selector expansion | Internal Only | 6 | Available | `DomStore.select()` resolves `#id`, `.class`, `*`, tag, compound simple selectors, selector lists, descendant combinators, child combinators, sibling combinators, `:scope`, `:has(...)`, `:lang(...)`, `:dir(...)`, `:not(...)`, `:is(...)`, `:where(...)`, `:focus`, `:focus-visible`, `:focus-within`, `:target`, bounded `:nth-*` forms including `of <selector-list>` support on the `nth-child`, `nth-last-child`, `nth-of-type`, and `nth-last-of-type` families, a bounded structural/state pseudo-class slice (`:root`, `:empty`, `:first-child`, `:last-child`, `:only-child`, `:first-of-type`, `:last-of-type`, `:only-of-type`, `:checked`, `:disabled`, `:enabled`, `:required`, `:optional`, `:link`, `:any-link`, `:placeholder-shown`, `:blank`, `:indeterminate`, `:default`, `:valid`, `:invalid`, `:in-range`, `:out-of-range`, `:read-only`, and `:read-write`), and `:defined` for the internal DOM slice. |
| DOM dump helpers | Internal Only | 1 | Available | `dumpDom()` is used for tree snapshots in tests and is not exposed publicly. |
| Harness read-only inspection | Stable Core | 1 | Available | `assertExists()` validates selector presence, `assertValue()` checks DOM text/value snapshots, `assertChecked()` checks form-control state, and `dumpDom()` returns the textual DOM snapshot. |
| Script runtime internals | Internal Only | 2 | Available | `src/script.zig` owns the lexer, parser, evaluator, and host-binding seam used during inline script bootstrap, including bootstrap-only `document.currentScript` / `document.readyState` / `document.onreadystatechange` state and deterministic `DOMContentLoaded` dispatch at completion. |
| Script runtime minimum slice | Stable Core | 2 | Available | Inline `<script>` bootstrapping can resolve `document.getElementById(...)`, `document.currentScript`, `document.readyState`, and `document.onreadystatechange`, observe deterministic `DOMContentLoaded` dispatch at completion, mutate `textContent` during harness construction, and report missing element access and unsupported syntax explicitly. |
| Global `Math` object | Stable Core | 4 | Available | Inline scripts can read `Math` / `window.Math` as a deterministic host object with the common constants `E`, `LN10`, `LN2`, `LOG10E`, `LOG2E`, `PI`, `SQRT1_2`, and `SQRT2`; `Math.random()` returns a deterministic sequence by default, and `HarnessBuilder.randomSeed(...)` can reseed that sequence before bootstrap. |
| Global `Crypto` object | Stable Core | 4 | Available | Inline scripts can read `crypto` / `window.crypto` as a deterministic host object with `randomUUID()`; `HarnessBuilder.randomSeed(...)` can reseed that deterministic sequence before bootstrap. |
| Script DOM query expansion | Stable Core | 7 | Available | Inline scripts and listeners can use `document.querySelector(...)`, `element.querySelector(...)`, `template.content.querySelector(...)`, `template.content.getElementById(...)`, `Element.matches(...)`, and `Element.closest(...)` through the shared DOM selector engine; misses return `null`, sibling combinators, `:scope`, `:has(...)`, `:lang(...)`, `:dir(...)`, `:not(...)`, `:is(...)`, `:where(...)`, `:focus`, `:focus-visible`, `:focus-within`, `:target`, `:defined`, bounded `:nth-*` forms including `of <selector-list>` support on the `nth-child`, `nth-last-child`, `nth-of-type`, and `nth-last-of-type` families, and the bounded structural/state pseudo-class slice including `:blank` work, and unsupported selector syntax fails explicitly. |
| Script DOM collection support | Stable Core | 7 | Available | Inline scripts and listeners can use `document.querySelectorAll(...)` and `element.querySelectorAll(...)`; results are minimal `NodeList` snapshots with `length`, `item(index)`, `forEach(callback[, thisArg])`, `keys()`, `values()`, and `entries()`. |
| Document and window aliases | Stable Core | 9 | Available | Inline scripts can read `document.documentElement`, `document.head`, `document.body`, `document.scrollingElement`, `document.activeElement`, `document.createRange()`, `document.defaultView`, `document.title`, `document.location`, `document.URL`, `document.documentURI`, `document.baseURI`, `document.compatMode`, `document.characterSet`, `document.charset`, `document.contentType`, `document.referrer`, `document.dir`, `document.domain`, `document.visibilityState`, `document.hidden`, `document.origin`, `document.hasFocus()`, `document.ownerDocument`, `document.parentNode`, `document.parentElement`, `document.firstElementChild`, `document.lastElementChild`, `document.childElementCount`, `Node.contains(...)`, `Node.compareDocumentPosition(...)`, `Node.isSameNode(...)`, `Node.isEqualNode(...)`, `Node.isConnected`, `Node.hasChildNodes()`, `Node.firstChild`, `Node.lastChild`, `Node.nextSibling`, `Node.previousSibling`, `Node.nextElementSibling`, and `Node.previousElementSibling`, `window.window`, `window.self`, `window.top`, `window.parent`, `window.opener`, `window.frameElement`, `window.closed`, `window.children`, `window.frames`, `window.length`, `window.navigator`, `window.performance`, `window.screen`, `window.devicePixelRatio`, `window.innerWidth`, `window.innerHeight`, `window.outerWidth`, `window.outerHeight`, `window.screenX`, `window.screenY`, `window.screenLeft`, `window.screenTop`, `window.scrollX`, `window.scrollY`, `window.pageXOffset`, `window.pageYOffset`, `window.name`, `window.title`, `window.location`, `window.origin`, `Element.baseURI`, `Element.origin`, `Element.ownerDocument`, `Element.parentNode`, `Element.parentElement`, `Element.firstElementChild`, `Element.lastElementChild`, and `Element.childElementCount`; `document.location` / `window.location` are `Location` host objects with `href`, `hash`, `protocol`, `host`, `hostname`, `port`, `username`, `password`, `pathname`, `search`, `assign()`, `replace()`, `reload()`, `toString()`, and `valueOf()`, fragment changes dispatch deterministic `hashchange` events through `window.addEventListener('hashchange', ...)` and `window.onhashchange`, history traversal dispatches deterministic `popstate` events through `window.addEventListener('popstate', ...)` and `window.onpopstate`, window focus transitions dispatch deterministic `focus` / `blur` events through `window.addEventListener('focus', ...)` / `window.onfocus` and `window.addEventListener('blur', ...)` / `window.onblur`, page lifecycle transitions dispatch deterministic `beforeunload` / `pagehide` / `unload` / `pageshow` events through `window.addEventListener('beforeunload', ...)` / `window.onbeforeunload`, `window.addEventListener('pagehide', ...)` / `window.onpagehide`, `window.addEventListener('unload', ...)` / `window.onunload`, and `window.addEventListener('pageshow', ...)` / `window.onpageshow`, scroll transitions dispatch deterministic `scroll` events through `document.onscroll` and `window.onscroll`, bootstrap completion dispatches deterministic `DOMContentLoaded` and `readystatechange` events through `document.addEventListener('DOMContentLoaded', ...)` and `document.onreadystatechange`, and deterministic `load` events through `window.addEventListener('load', ...)` and `window.onload`, while `window.navigator` is a limited read-only host object with `userAgent`, `appCodeName`, `appName`, `appVersion`, `product`, `productSub`, `vendor`, `vendorSub`, `mimeTypes` (`length`, `item(index)`, `namedItem(name)`, `keys()`, `values()`, `entries()`, `toString()`), `languages` (`length`, `item(index)`, `contains(value)`, `keys()`, `values()`, `entries()`, `toString()`), `userLanguage`, `browserLanguage`, `systemLanguage`, `oscpu`, `pdfViewerEnabled`, `doNotTrack`, `javaEnabled()`, `plugins` (`length`, `item(index)`, `namedItem(name)`, `refresh()`, `toString()`), `platform`, `language`, `cookieEnabled`, `onLine`, `webdriver`, `hardwareConcurrency`, and `maxTouchPoints`, `window.performance` is a deterministic clock-backed host object with `now()` / `timeOrigin`, `document.defaultView` / `window.window` / `window.self` / `window.top` / `window.parent` are deterministic `Window` aliases, `window.opener` is `null`, `window.frameElement` is `null`, `window.closed` is `false`, `document.visibilityState` is `visible`, `document.hidden` is `false`, `document.hasFocus()` is deterministic, viewport metrics are fixed, screen-position aliases are fixed at `0`, `window.screen` is a deterministic read-only `Screen` host object with `orientation.type` and `orientation.angle`, and assignments to `document.title`, `document.location`, `document.dir`, and `window.name` stay wired into the copied session state. |
| Navigator collection helpers | Stable Core | 9 | Available | Inline scripts can read `window.navigator.mimeTypes`, `window.navigator.languages`, and `window.navigator.plugins`; `mimeTypes` and `plugins` are `PluginArray`-like surfaces with `length`, `item(index)`, `namedItem(name)`, `keys()`, `values()`, `entries()`, `forEach(callback[, thisArg])`, `refresh()` on `plugins`, and `toString()`, while `languages` is a `DOMStringList`-like surface with `length`, `item(index)`, `contains(value)`, `keys()`, `values()`, `entries()`, `forEach(callback[, thisArg])`, and `toString()`, plus the deterministic legacy aliases `userLanguage`, `browserLanguage`, `systemLanguage`, and `oscpu`. |
| Document cookie | Stable Core | 9 | Available | Inline scripts can read and set `document.cookie`; the workspace keeps a deterministic cookie jar in session state, and malformed assignments fail explicitly. |
| CSSOM View computed style view | Stable Core | 9 | Available | Inline scripts can use `window.getComputedStyle(element[, pseudoElt])` as a minimal live read-only `CSSStyleDeclaration` view backed by the element's inline style attribute; it supports `cssText`, `getPropertyValue(...)`, `getPropertyPriority(...)`, `length`, and `item(index)`, while write attempts and unsupported pseudo-elements fail explicitly. |
| CSSOM View geometry snapshot | Stable Core | 9 | Available | Inline scripts can use `Element.getBoundingClientRect()` as a minimal read-only `DOMRect` snapshot derived from the element's inline style attribute, and `Element.getClientRects()` as a minimal read-only `DOMRectList` snapshot over the same geometry; `Element.scrollIntoView()` routes through the deterministic scroll mock and scrolls to origin instead of doing layout-aware positioning; the snapshot objects expose `x`, `y`, `width`, `height`, `top`, `right`, `bottom`, `left`, `length`, `item(index)`, and `String(...)` / `toString()`, while the workspace does not attempt full layout or viewport geometry. |
| Window history navigation | Stable Core | 10 | Available | Inline scripts can read `window.history`; the limited `History` host object supports `length`, `state`, `scrollRestoration`, `back()`, `forward()`, `go(delta)`, `pushState(state, title, url)`, and `replaceState(state, title, url)`, keeps `state` payload tracking intentionally minimal (`null` / `undefined` stay `null`, while other values are stringified in this workspace), and dispatches deterministic `popstate` events on history traversal through `window.addEventListener('popstate', ...)` and `window.onpopstate`. |
| Universal selector support | Internal Only | 6 | Available | `*` matches every element in tree order and works in compound selectors and the shared script-side query APIs. |
| Document scripts live collection | Stable Core | 8 | Available | Inline scripts can use `document.scripts`; the live HTMLCollection surface supports `length`, `item(index)`, `namedItem(name)`, `keys()`, `values()`, and `entries()`, and reflects DOM mutation in document order. |
| Document anchors live collection | Stable Core | 8 | Available | Inline scripts can use `document.anchors`; the live HTMLCollection surface supports `length`, `item(index)`, `namedItem(name)`, `keys()`, `values()`, and `entries()`, and only includes `<a>` descendants with a `name` attribute. |
| Document forms live collection | Stable Core | 8 | Available | Inline scripts can use `document.forms`; the live HTMLCollection surface supports `length`, `item(index)`, `namedItem(name)`, `forEach(callback[, thisArg])`, `keys()`, `values()`, and `entries()`, and reflects DOM mutation in document order. |
| Form elements live collection | Stable Core | 8 | Available | Inline scripts can use `form.elements`; the live HTMLCollection surface supports `length`, `item(index)`, `namedItem(name)`, `forEach(callback[, thisArg])`, `keys()`, `values()`, and `entries()`, `form.length` mirrors the same live count, the collection stays in document order, and it includes controls associated via `form=` outside the subtree; `namedItem(name)` returns a `RadioNodeList` when multiple matching controls share a name. |
| RadioNodeList value semantics | Stable Core | 8 | Available | Inline scripts can assign `RadioNodeList.value` and call `forEach(callback[, thisArg])`; matching radio values check the first matching radio in tree order, and missing values clear the group in this workspace. |
| Text selection state on form controls | Stable Core | 3 | Available | Inline scripts can read `selectionStart`, `selectionEnd`, and `selectionDirection` on supported `input` / `textarea` controls, call `setSelectionRange(...)`, `setRangeText(...)`, and `select()`, observe document-level `selectionchange` handlers, use minimal `window.getSelection()` / `document.getSelection()` snapshots for the active text control, and call `collapseToStart()` / `collapseToEnd()` / `containsNode(...)` / `removeAllRanges()` / `collapse(node, offset)` / `extend(node, offset)` / `setBaseAndExtent(...)` / `deleteFromDocument()` / `setPosition(node, offset)` / `selectAllChildren(node)` / `addRange(range)` / `removeRange(range)` / `getRangeAt(0)` / `Range.cloneRange()` / `Range.cloneContents()` / `Range.createContextualFragment()` / `Range.selectNode()` / `Range.selectNodeContents()` / `Range.setStart()` / `Range.setEnd()` / `Range.collapse([toStart])` / `Range.insertNode()` / `Range.surroundContents()` / `Range.deleteContents()` / `Range.isPointInRange(...)` / `Range.comparePoint(...)` / `Range.compareBoundaryPoints(...)` / `Range.extractContents()` on those snapshots; unsupported controls read as `null` and reject selection mutations explicitly. |
| Numeric step controls | Stable Core | 3 | Available | Inline scripts can call `stepUp(...)` / `stepDown(...)` on supported `input[type=number]` / `input[type=range]` / `input[type=date]` / `input[type=datetime-local]` / `input[type=time]` / `input[type=month]` / `input[type=week]` controls; the workspace uses the reflected `min` / `max` / `step` / `value` state and rejects unsupported controls explicitly. |
| Numeric/date `valueAsNumber` / `valueAsDate` controls | Stable Core | 3 | Available | Inline scripts can read and write `input.valueAsNumber` on supported `number` / `range` / `date` / `datetime-local` / `time` / `month` / `week` controls and `input.valueAsDate` on supported `date` / `datetime-local` / `time` / `month` / `week` / `range` controls; `range` values are canonicalized through the same step-aware normalization as `value`, unsupported controls read as `NaN` / `null`, and unsupported setters reject explicitly. |
| Form control validity methods | Stable Core | 3 | Available | Inline scripts can call `checkValidity()` / `reportValidity()` on `input`, `textarea`, `select`, and `form`; `reportValidity()` also dispatches deterministic `invalid` events on invalid controls; supported form controls also expose `setCustomValidity()` / `validationMessage` / `willValidate` / `validity`, they follow the same minimal built-in constraint state that powers `:valid` / `:invalid`, including `typeMismatch` for email/url inputs, and unsupported elements reject the methods explicitly. |
| Form submission reflection | Stable Core | 8 | Available | Inline scripts can read and write `form.action`, `form.method`, `form.enctype`, `form.encoding`, `form.target`, `form.acceptCharset`, `form.name`, `form.rel`, `form.relList`, and submit-control overrides `formAction` / `formMethod` / `formEnctype` / `formTarget` on `input` and `button`; action URLs resolve against the current location, method/enctype stay limited to known values, `form.relList.add()` / `remove()` / `toggle()` / `replace()` / `supports()` are available with `supports()` limited to `noreferrer` / `noopener` / `opener`, `form.submit()` / `form.requestSubmit()` / `form.reset()` dispatch `submit` / `reset` events deterministically, `SubmitEvent.submitter` reflects the explicit submitter on submit events, and actual submission/navigation is still mock-driven. |
| FormData and formdata event | Stable Core | 3 | Available | Inline scripts can construct `new FormData(form)` and listen for `formdata` after a non-canceled submit; `FormData` supports `append()`, `delete()`, `get()`, `getAll()`, `has()`, `set()`, `keys()`, `values()`, `entries()`, `forEach(callback[, thisArg])`, and `toString()`, and `event.formData` exposes the captured payload. |
| Dialog element controls | Stable Core | 8 | Available | Inline scripts can use `HTMLDialogElement.open`, `HTMLDialogElement.returnValue`, `HTMLDialogElement.closedBy`, `show()`, `showModal()`, `requestClose([returnValue])`, and `close([returnValue])`; the slice toggles the `open` attribute and dispatches deterministic `cancel` / `close` events, but it does not model top-layer rendering or blocking modal behavior. |
| Details element controls | Stable Core | 8 | Available | Inline scripts can use `HTMLDetailsElement.name` and `HTMLDetailsElement.open`; the slice reflects the `name` and `open` attributes, clicking the first `summary` child toggles `open`, and deterministic `toggle` events are dispatched, but details-name-group exclusivity is not modeled. |
| Popover element controls | Stable Core | 8 | Available | Inline scripts can use `popover`, `showPopover()`, `hidePopover()`, `togglePopover()`, `popoverTargetElement`, `popoverTargetAction`, and the `:popover-open` selector state; showing, hiding, and target activation dispatch deterministic `toggle` events, but top-layer rendering and light-dismiss behavior are not modeled. |
| Form owner reflection | Stable Core | 8 | Available | Inline scripts can read `form` on form-associated controls (`input`, `button`, `textarea`, `select`, `fieldset`, `option`, `optgroup`, `output`, `meter`, `progress`, and `object`) and on `legend` when it is a direct child of a `fieldset`; the property resolves the explicit `form` attribute first and otherwise falls back to the nearest owning `form` or `select` chain, while unsupported elements reject it explicitly. |
| HTMLFieldSetElement reflection | Stable Core | 8 | Available | Inline scripts can use `HTMLFieldSetElement.type`, `disabled`, `form`, `name`, and `elements`; `type` returns `"fieldset"` and unsupported elements reject the surface explicitly. |
| HTMLOutputElement reflection | Stable Core | 8 | Available | Inline scripts can use `HTMLOutputElement.type`, `defaultValue`, `value`, `htmlFor`, `form`, `labels`, `willValidate`, `validity`, `validationMessage`, `checkValidity()`, `reportValidity()`, and `setCustomValidity()`; `htmlFor` is a `DOMTokenList` whose `value` forwards to the `for` attribute, `defaultValue` follows the output override semantics, and `value` reflects descendant text content. |
| HTMLObjectElement reflection | Stable Core | 8 | Available | Inline scripts can use `HTMLObjectElement.data`, `type`, `name`, `width`, `height`, `useMap`, `form`, `contentDocument`, `contentWindow`, `getSVGDocument()`, `willValidate`, `validity`, `validationMessage`, `checkValidity()`, `reportValidity()`, and `setCustomValidity()`; the embedded browsing-context getters are modeled as `null` in this workspace, and unsupported elements reject the surface explicitly. |
| HTMLProgressElement / HTMLMeterElement reflection | Stable Core | 8 | Available | Inline scripts can use `HTMLProgressElement.value`, `max`, `position`, `form`, and `labels`, plus `HTMLMeterElement.value`, `min`, `max`, `low`, `high`, `optimum`, `form`, and `labels`; these are modern numeric reflections, and unsupported elements reject them explicitly. |
| File input selection surface | Stable Core | 8 | Available | Inline scripts can read a minimal read-only `input.files` snapshot on `input[type=file]`; the list reflects `Harness.setFiles(...)` selections and exposes `length`, `item(index)`, `keys()`, `values()`, `entries()`, `forEach(callback[, thisArg])`, and `toString()` while unsupported elements reject it explicitly. |
| Input datalist association | Stable Core | 8 | Available | Inline scripts can use `input.list` to resolve the associated `datalist` element by `id`; the property returns the `datalist` element or `null`, and unsupported elements reject it explicitly. |
| Input indeterminate state | Stable Core | 8 | Available | Inline scripts can read and write `input.indeterminate` on supported inputs; it feeds the existing `:indeterminate` selector state, and unsupported elements reject it explicitly. |
| Input accept, size, capture, color, and image reflection | Stable Core | 8 | Available | Inline scripts can read and write `input.accept`, `input.size`, and `input.capture` on supported inputs; `accept` and `capture` are reflected as raw file-input hint strings and `size` mirrors the positive integer character-width field, `input[type=color]` also exposes `alpha` and `colorSpace`, `input` type=image also exposes `src`, `alt`, `useMap`, `width`, and `height`, and unsupported elements reject it explicitly. |
| Textarea rows, cols, and wrap | Stable Core | 8 | Available | Inline scripts can read and write `textarea.rows`, `textarea.cols`, and `textarea.wrap` on supported textareas; `rows` and `cols` use positive-integer reflection with fallback defaults of 2 and 20, `wrap` reflects the soft/hard wrap state, and unsupported elements reject them explicitly. |
| Input/select picker surface | Stable Core | 8 | Available | Inline scripts can call `showPicker()` on supported `input` and `select` elements; the method is a deterministic no-op in this workspace and unsupported elements reject it explicitly. |
| Select options live collection | Stable Core | 8 | Available | Inline scripts can use `select.options`; the live HTMLCollection surface supports `length`, `item(index)`, `namedItem(name)`, `forEach(callback[, thisArg])`, `keys()`, `values()`, and `entries()`, `select.length` mirrors the same live count, `select.selectedIndex` stays aligned with `select.value` / `select.selectedOptions`, `select.size` mirrors the select sizing hint, `select.type` mirrors the select mode (`select-one` / `select-multiple`), accepts `add(option)` / `remove(index)` mutation helpers, and reflects option DOM mutation in tree order. |
| Select element mutation | Stable Core | 8 | Available | Inline scripts can use `HTMLSelectElement.add(option[, before])` / `HTMLSelectElement.remove(index?)` to mutate the same live option state as `select.options.add(option)` / `select.options.remove(index)`. |
| Select selected options live collection | Stable Core | 8 | Available | Inline scripts can use `select.selectedOptions`; the live HTMLCollection surface supports `length`, `item(index)`, `namedItem(name)`, `forEach(callback[, thisArg])`, `keys()`, `values()`, and `entries()`, and reflects the currently selected option DOM state. |
| Option and optgroup reflection | Stable Core | 8 | Available | Inline scripts can read and write `HTMLOptionElement.label`, `HTMLOptionElement.defaultSelected`, `HTMLOptionElement.text`, and `HTMLOptionElement.index`, plus `HTMLOptGroupElement.label`; option `label` falls back to text content when absent, `defaultSelected` reflects the `selected` attribute, `text` aliases `textContent`, and `index` reports the owning select position or `0` when detached. |
| Fieldset elements live collection | Stable Core | 8 | Available | Inline scripts can use `fieldset.elements`; the live HTMLCollection surface supports `length`, `item(index)`, `namedItem(name)`, `forEach(callback[, thisArg])`, `keys()`, `values()`, and `entries()`, and reflects descendant form controls in tree order. |
| Datalist options live collection | Stable Core | 8 | Available | Inline scripts can use `datalist.options`; the live HTMLCollection surface supports `length`, `item(index)`, `namedItem(name)`, `forEach(callback[, thisArg])`, `keys()`, `values()`, and `entries()`, and reflects option descendants in tree order. |
| Map name and areas live collection | Stable Core | 8 | Available | Inline scripts can use `map.name` and `map.areas`; the `name` reflected string property reads and writes the `name` attribute while the live HTMLCollection surface supports `length`, `item(index)`, `namedItem(name)`, `forEach(callback[, thisArg])`, `keys()`, `values()`, and `entries()`, and reflects descendant `area` elements in tree order. |
| Table bodies live collection | Stable Core | 8 | Available | Inline scripts can use `table.tBodies`; the live HTMLCollection surface supports `length`, `item(index)`, `namedItem(name)`, `forEach(callback[, thisArg])`, `keys()`, `values()`, and `entries()`, and reflects direct child `tbody` elements in tree order. |
| Element labels live collection | Stable Core | 8 | Available | Inline scripts can use `element.labels` on labelable form controls and `fieldset`; the live NodeList surface supports `length`, `item(index)`, `forEach(callback[, thisArg])`, `keys()`, `values()`, and `entries()`, and reflects explicit `label[for]` associations plus implicit ancestor labels in tree order. |
| Label control association | Stable Core | 8 | Available | Inline scripts can use `HTMLLabelElement.htmlFor`, `HTMLLabelElement.control`, and `HTMLLabelElement.form`; `htmlFor` reflects the `for` attribute, `control` resolves the explicit `for` target or the first labelable descendant in tree order, and `form` follows the labeled control's form owner when one exists. |
| Stylesheet owner link referrer policy | Stable Core | 8 | Available | Inline scripts can use `HTMLLinkElement.referrerPolicy`; the reflected string property reads and writes the `referrerpolicy` attribute while leaving `link.sheet` linkage untouched. |
| Stylesheet owner link as reflection | Stable Core | 8 | Available | Inline scripts can use `HTMLLinkElement.as`; the reflected string property reads and writes the `as` attribute while leaving `link.sheet` linkage untouched. |
| Stylesheet owner link charset reflection | Stable Core | 8 | Available | Inline scripts can use `HTMLLinkElement.charset`; the reflected string property reads and writes the `charset` attribute while leaving `link.sheet` linkage untouched. |
| Stylesheet owner link responsive image metadata | Stable Core | 8 | Available | Inline scripts can use `HTMLLinkElement.imageSrcset` and `HTMLLinkElement.imageSizes`; the reflected string properties read and write the `imagesrcset` and `imagesizes` attributes while leaving `link.sheet` linkage untouched. |
| Stylesheet owner link fetch priority reflection | Stable Core | 8 | Available | Inline scripts can use `HTMLLinkElement.fetchPriority`; the reflected string property reads and writes the `fetchpriority` attribute while leaving `link.sheet` linkage untouched. |
| Document media and legacy live collections | Stable Core | 8 | Available | Inline scripts can use `document.images`, `document.links`, `document.embeds`, `document.plugins`, `document.applets`, and `document.all`; the live HTMLCollection surfaces support `length`, `item(index)`, `namedItem(name)`, `forEach(callback[, thisArg])`, `keys()`, `values()`, and `entries()`, with `images` tracking `img`, `links` tracking `a[href]` / `area[href]`, `embeds` and `plugins` tracking `embed`, `applets` tracking `applet`, and `all` tracking every element in document order. |
| HTMLImageElement reflection | Stable Core | 8 | Available | Inline scripts can use `HTMLImageElement.src`, `srcset`, `sizes`, `width`, `height`, `loading`, `decoding`, `currentSrc`, `complete`, `naturalWidth`, `naturalHeight`, `fetchPriority`, `crossOrigin`, `referrerPolicy`, `alt`, `useMap`, and `isMap`; the reflected string, numeric, and boolean properties read and write the corresponding attributes while unsupported elements reject them explicitly. |
| HTMLCanvasElement reflection | Stable Core | 8 | Available | Inline scripts can use `HTMLCanvasElement.width`, `height`, deterministic `getContext(contextId[, options])`, deterministic `toDataURL(type[, quality])` placeholder output, and deterministic `toBlob(callback[, type[, quality]])` callback delivery with a `null` blob payload; the width and height IDL attributes reflect non-negative integer content attributes with defaults of `300` and `150`, `getContext()` currently returns `null`, `toDataURL()` returns a placeholder data URL, `toBlob()` invokes the callback synchronously with `null`, and bitmap rendering / `transferControlToOffscreen()` are not modeled. |
| HTMLSlotElement reflection | Stable Core | 8 | Available | Inline scripts can use `HTMLSlotElement.name` plus deterministic `assignedNodes([options])` / `assignedElements([options])` snapshots over fallback-content children; shadow-tree distribution and flattening behavior are not modeled. |
| HTMLMediaElement / HTMLAudioElement / HTMLVideoElement reflection | Stable Core | 8 | Available | Inline scripts can use `HTMLMediaElement.src`, `currentSrc`, `currentTime`, `buffered`, `seekable`, `played`, `textTracks`, `duration`, `paused`, `seeking`, `ended`, `readyState`, `networkState`, `crossOrigin`, `preload`, `autoplay`, `loop`, `controls`, `defaultMuted`, `muted`, `volume`, `defaultPlaybackRate`, `playbackRate`, `preservesPitch`, `disableRemotePlayback`, and `controlsList`, plus deterministic `load()`, `pause()`, and `canPlayType(type)` methods; `HTMLVideoElement` also exposes `disablePictureInPicture`, `poster`, `playsInline`, `width`, and `height`; the reflected string, numeric, and boolean properties read and write the corresponding attributes or deterministic playback state while unsupported elements reject them explicitly. |
| HTMLSourceElement reflection | Stable Core | 8 | Available | Inline scripts can use `HTMLSourceElement.src`, `srcset`, `sizes`, `media`, and `type`; the reflected string properties read and write the corresponding attributes while unsupported elements reject them explicitly. |
| Blocking attribute reflection | Stable Core | 8 | Available | Inline scripts can use `HTMLLinkElement.blocking`, `HTMLScriptElement.blocking`, and `HTMLStyleElement.blocking`; the reflected `DOMTokenList` currently recognizes `render` as the supported token, while unsupported elements reject the surface explicitly and render-blocking behavior itself is not modeled. |
| HTMLTrackElement reflection | Stable Core | 8 | Available | Inline scripts can use `HTMLTrackElement.kind`, `src`, `srclang`, `label`, `default`, `readyState`, and `track`; the reflected string and boolean properties read and write the corresponding attributes, `track` returns a minimal `TextTrack` snapshot with `kind`, `label`, `language`, `mode`, and `readyState`, and unsupported elements reject the surface explicitly. |
| HTMLDataElement / HTMLTimeElement reflection | Stable Core | 8 | Available | Inline scripts can use `HTMLDataElement.value` and `HTMLTimeElement.dateTime`; `data.value` reflects the machine-readable value, `time.dateTime` reflects the `datetime` content attribute with a text-content fallback, and unsupported elements reject the surface explicitly. |
| HTMLQuoteElement / HTMLModElement reflection | Stable Core | 8 | Available | Inline scripts can use `HTMLQuoteElement.cite` on `blockquote` / `q` and `HTMLModElement.cite` / `HTMLModElement.dateTime` on `ins` / `del`; the reflected metadata reads and writes the corresponding attributes, and unsupported elements reject the surface explicitly. |
| HTMLTemplateElement declarative shadow-root flags | Stable Core | 8 | Available | Inline scripts can use `HTMLTemplateElement.shadowRootMode`, `shadowRootDelegatesFocus`, `shadowRootClonable`, `shadowRootSerializable`, and `shadowRootCustomElementRegistry`; the flags reflect the corresponding `<template>` content attributes, while `template.content` remains the existing inert `DocumentFragment` slice. |
| HTMLIFrameElement reflection | Stable Core | 8 | Available | Inline scripts can use `HTMLIFrameElement.src`, `srcdoc`, `name`, `loading`, `referrerPolicy`, `allow`, `sandbox`, `allowFullscreen`, `credentialless`, `width`, `height`, `fetchPriority`, `contentDocument`, and `contentWindow`; the reflected string, boolean, and token-list properties read and write the corresponding attributes while the embedded browsing-context getters are modeled as `null` in this workspace and unsupported elements reject the surface explicitly. |
| Document stylesheet live collection | Stable Core | 8 | Available | Inline scripts can use `document.styleSheets`; the live StyleSheetList surface supports `length`, `item(index)`, `forEach(callback[, thisArg])`, `keys()`, `values()`, and `entries()`, and reflects `<style>` elements plus `link rel=stylesheet` owners in document order, while `CSSStyleSheet.cssRules` exposes a minimal simple-qualified-rule parser plus bounded `@media` / `@supports` / `@supports-condition` / `@document` / `@container` / `@starting-style` / `@position-try` / `@scope` / `@keyframes` / `@font-face` / `@font-feature-values` / `@font-palette-values` rules with `CSSFontPaletteValuesRule` exposing `name`, `fontFamily`, `basePalette`, `overrideColors`, and writable `CSSFontPaletteValuesRule.cssText` / `@color-profile` / `@page` / `@layer` block / statement rules / `@property` block rules with writable `CSSPropertyRule.cssText` / `@counter-style` rules with `name`, `system`, `symbols`, `negative`, `prefix`, `suffix`, `range`, `pad`, `fallback`, `speakAs`, `additiveSymbols`, and writable `CSSCounterStyleRule.cssText`, plus `@charset` / `@import` / `@namespace` rules for `<style>` owners, its `CSSRuleList` also supports `forEach(callback[, thisArg])`, `CSSStyleRule.style` is exposed as a live writable `CSSStyleDeclaration`, writable top-level `CSSStyleRule.selectorText` and `CSSStyleRule.cssText`, `CSSFontFaceRule.style, writable `CSSFontFaceRule.cssText`, and writable `CSSKeyframeRule.keyText`, `CSSPageRule.selectorText` for top-level `@page` rules, `CSSPageRule.cssText`, `CSSMediaRule.cssText`, `CSSMediaRule.insertRule()` / `deleteRule()` on top-level `@media` rules, writable `CSSPageRule.style`, `CSSRule.type` exposes the legacy CSSOM integer mapping for classic rule kinds (with newer at-rules returning `0`), `CSSRule.parentStyleSheet` returns the owning stylesheet on rule objects, `CSSRule.parentRule` returns the immediate owning rule on nested rule objects, `CSSStyleSheet.ownerNode` returns the owner element, `CSSStyleSheet.href` / `CSSStyleSheet.title` / `CSSStyleSheet.disabled` / `CSSStyleSheet.media` expose owner metadata, `CSSStyleSheet.media.mediaText` is writable and `CSSStyleSheet.media.appendMedium()` / `deleteMedium()` are available on stylesheet media lists, `CSSStyleSheet.disabled` toggles the owner element's `disabled` attribute, and `HTMLStyleElement.sheet` / `HTMLLinkElement.sheet` return the stylesheet owner linkage with reflected `media`, `rel`, `relList`, `relList.supports()`, `relList.replace()`, `type`, `hreflang`, `crossOrigin`, and `disabled`, with `CSSMediaRule.media` is writable and `CSSImportRule.media` remains read-only as the same minimal `MediaList` surface, while `CSSImportRule.styleSheet` / `CSSStyleSheet.ownerRule` are exposed as null linkage surfaces and `CSSImportRule.supportsText` / `CSSImportRule.layerName` expose read-only metadata, and `CSSSupportsRule.insertRule()` / `CSSSupportsRule.deleteRule()` and `CSSContainerRule.insertRule()` / `CSSContainerRule.deleteRule()` are available on top-level `@supports` / `@container` rules, alongside legacy `CSSStyleSheet.rules` / `addRule()` / `removeRule()` aliases available alongside `CSSStyleSheet.insertRule()` / `deleteRule()` / `replaceSync()` on inline `<style>` owners. `CSSFontFeatureValuesRule.cssText` and `CSSColorProfileRule.cssText` are writable too. |
| Link rel token list methods | Stable Core | 8 | Available | `HTMLAnchorElement.relList`, `HTMLAreaElement.relList`, and stylesheet-owner `relList` surfaces expose `add()`, `remove()`, `toggle()`, `replace()`, and `supports()` on the same token store. |
| CSSRuleList iterator parity | Stable Core | 8 | Available | Inline scripts can use `document.styleSheets.item(0).cssRules` as a live `CSSRuleList` with `length`, `item(index)`, `keys()`, `values()`, `entries()`, and `forEach(callback[, thisArg])` parity; iterator values are current rule snapshots and callbacks receive the live rule list. |
| CSS starting-style / position-try mutation | Stable Core | 8 | Available | Inline scripts can use `CSSStartingStyleRule.cssText` and `CSSPositionTryRule.cssText` on `@starting-style` / `@position-try` rules; assignments rewrite the owning block in place while preserving the live rule snapshot. |
| Nested keyframe mutation | Stable Core | 8 | Available | Inline scripts can use `CSSKeyframeRule.keyText` on nested `@keyframes` rules and writable `CSSKeyframesRule.cssText`; `CSSKeyframesRule.appendRule()`, `deleteRule()`, and `findRule()` mutate the nested keyframe list. |
| CSS container rule metadata | Stable Core | 8 | Available | Inline scripts can use `CSSContainerRule.containerName` and `CSSContainerRule.containerQuery` on `@container` rules; `CSSContainerRule.conditionText` and `CSSContainerRule.cssText` are writable and rewrite the owning block in place while preserving nested rules, `CSSContainerRule.insertRule()` / `deleteRule()` mutate the nested rule list on top-level `@container` rules, and `cssRules` remains available. |
| CSS media rule match and condition text state | Stable Core | 8 | Available | Inline scripts can use `CSSMediaRule.matches` on `@media` rules; it mirrors the current seeded `window.matchMedia(...)` match state, `CSSMediaRule.conditionText` is writable and rewrites the owning block in place while preserving nested rules, and `CSSMediaRule.insertRule()` / `deleteRule()` mutate the nested rule list on top-level `@media` rules. |
| CSS document rule condition text and cssText mutation | Stable Core | 8 | Available | Inline scripts can use `CSSDocumentRule.conditionText` and `CSSDocumentRule.cssText` on `@document` rules; assignments rewrite the owning block in place while preserving nested rules. |
| CSS supports rule condition text mutation | Stable Core | 8 | Available | Inline scripts can use `CSSSupportsRule.conditionText` and `CSSSupportsRule.cssText` on `@supports` rules; assignments rewrite the owning block in place while preserving nested rules, and `CSSSupportsRule.insertRule()` / `deleteRule()` mutate the nested rule list on top-level `@supports` rules. |
| CSS supports-condition rule metadata | Stable Core | 8 | Available | Inline scripts can use `CSSSupportsConditionRule.name` on `@supports-condition` rules; `cssText` is writable and rewrites the owning block in place while `cssRules` remains an empty deterministic slice for now. |
| CSS layer nameText/cssText mutation | Stable Core | 8 | Available | Inline scripts can use `CSSLayerBlockRule.nameText` / `CSSLayerStatementRule.nameText` and writable `CSSLayerBlockRule.cssText` / `CSSLayerStatementRule.cssText` on `@layer` rules; assignments rewrite the owning block in place while preserving nested rules, and `CSSLayerStatementRule.nameList` exposes the comma-separated names as a `DOMStringList`. |
| CSS scope rule metadata | Stable Core | 8 | Available | Inline scripts can use `CSSScopeRule.start` and `CSSScopeRule.end` on `@scope` rules; the getters expose the scope root and scope limit strings (or `null` when absent), and `CSSScopeRule.cssText` / `CSSScopeRule.cssRules` remain available on the same slice. |
| Table rows and cells live collection | Stable Core | 8 | Available | Inline scripts can use `table.rows`, `tbody.rows`, `thead.rows`, `tfoot.rows`, and `tr.cells`; the live HTMLCollection surfaces support `length`, `item(index)`, `namedItem(name)`, `forEach(callback[, thisArg])`, `keys()`, `values()`, and `entries()`, with table rows reflecting direct `tr` children plus rows in direct `thead` / `tbody` / `tfoot` children, `tr.cells` reflecting direct `td` / `th` children, and `HTMLTableRowElement.rowIndex` / `sectionRowIndex` plus `HTMLTableCellElement.cellIndex` and `HTMLTableCellElement.colSpan` / `rowSpan` / `headers` plus `HTMLTableHeaderCellElement.scope` / `abbr` exposing the same live positions and reflection state. |
| Table columns live reflection | Stable Core | 8 | Available | Inline scripts can use `HTMLTableColElement.span`, `HTMLTableColElement.width`, `HTMLTableColElement.align`, `HTMLTableColElement.ch`, `HTMLTableColElement.chOff`, `HTMLTableColElement.vAlign`, and `HTMLTableColElement.bgColor` on `col` / `colgroup` elements; the reflected properties stay in sync with the corresponding attributes and reject unsupported elements explicitly. |
| Table section row mutation | Stable Core | 8 | Available | Inline scripts can use `thead.insertRow([index])` / `thead.deleteRow([index])`, `tbody.insertRow([index])` / `tbody.deleteRow([index])`, and `tfoot.insertRow([index])` / `tfoot.deleteRow([index])`; the helpers mutate the same live section row collections and stay bounded to `thead` / `tbody` / `tfoot` elements. |
| Table row and cell mutation | Stable Core | 8 | Available | Inline scripts can use `HTMLTableElement.insertRow([index])` / `HTMLTableElement.deleteRow([index])` and `HTMLTableRowElement.insertCell([index])` / `HTMLTableRowElement.deleteCell([index])`; the helpers mutate the same live row/cell collections and stay bounded to `table` / `tr` elements. |
| Table section access and mutation | Stable Core | 8 | Available | Inline scripts can use `table.caption`, `table.tHead`, `table.tFoot`, `HTMLTableElement.createCaption()` / `deleteCaption()`, `HTMLTableElement.createTHead()` / `deleteTHead()`, `HTMLTableElement.createTBody()`, and `HTMLTableElement.createTFoot()` / `deleteTFoot()`; the live section getters reflect direct `caption` / `thead` / `tfoot` children and the create/delete helpers stay bounded to `table` elements. |
| Table legacy reflection | Stable Core | 8 | Available | Inline scripts can use `HTMLTableElement.align`, `HTMLTableElement.border`, `HTMLTableElement.frame`, `HTMLTableElement.rules`, `HTMLTableElement.summary`, `HTMLTableElement.width`, `HTMLTableElement.bgColor`, `HTMLTableElement.cellPadding`, and `HTMLTableElement.cellSpacing`, plus legacy reflected `align` / `ch` / `chOff` / `vAlign` on `HTMLTableSectionElement`, legacy reflected `align` / `ch` / `chOff` / `vAlign` / `bgColor` on `HTMLTableRowElement`, and legacy reflected `align` / `axis` / `height` / `width` / `ch` / `chOff` / `noWrap` / `vAlign` / `bgColor` on `HTMLTableCellElement`; the reflected properties stay in sync with the corresponding attributes and `bgColor` / `cellPadding` / `cellSpacing` use legacy null-to-empty string behavior. |
| Element and document tag/class/namespace live collections | Stable Core | 8 | Available | Inline scripts can use `getElementsByTagName`, `getElementsByTagNameNS`, and `getElementsByClassName` on `Document` and `Element`; the live HTMLCollection surfaces support `length`, `item(index)`, `namedItem(name)`, `forEach(callback[, thisArg])`, `keys()`, `values()`, and `entries()`, and reflect descendant elements in tree order with tag-name, namespace, and class-token filtering. |
| Document named elements live collection | Stable Core | 8 | Available | Inline scripts can use `document.getElementsByName`; the live NodeList surface supports `length`, `item(index)`, `forEach(callback[, thisArg])`, `keys()`, `values()`, and `entries()`, and reflects descendant elements with matching `name` attributes in tree order. |
| Child-element live collections | Stable Core | 8 | Available | Inline scripts can use `Element.children`, `document.children`, and `template.content.children`; the live HTMLCollection surface supports `length`, `item(index)`, `namedItem(name)`, `forEach(callback[, thisArg])`, `keys()`, `values()`, and `entries()`, and reflects DOM mutation in tree order. |
| Child-node live collections | Stable Core | 8 | Available | Inline scripts can use `document.childNodes`, `element.childNodes`, and `template.content.childNodes`; the live NodeList surface supports `length`, `item(index)`, `forEach(callback[, thisArg])`, `keys()`, `values()`, and `entries()`, and node wrappers expose `nodeName`, `nodeType`, `namespaceURI`, `nodeValue`, `data`, `textContent`, `length`, `wholeText`, and CharacterData editing methods (`splitText()`, `substringData()`, `appendData()`, `insertData()`, `deleteData()`, and `replaceData()`); `template.content` also exposes `firstElementChild`, `lastElementChild`, `childElementCount`, `isConnected`, and the same detached fragment sibling helpers. |
| Element text aliases | Stable Core | 8 | Available | Inline scripts can use `Element.innerText` and `Element.outerText` as deterministic `textContent`-like aliases on Element nodes; getters and setters route through the same shared text store, `outerText` replaces the element in its parent with a text node, and unsupported nodes reject the properties explicitly. |
| Script DOM attribute reflection | Stable Core | 8 | Available | Inline scripts can use `getAttribute(...)`, `getAttributeNS(...)`, `getAttributeNode(...)`, `getAttributeNodeNS(...)`, `setAttributeNode(...)`, `setAttributeNodeNS(...)`, `removeAttributeNode(...)`, `setAttribute(...)`, `setAttributeNS(...)`, `removeAttribute(...)`, `removeAttributeNS(...)`, `hasAttribute(...)`, `hasAttributeNS(...)`, `hasAttributes()`, `getAttributeNames()`, and `toggleAttribute(...)`; `Element.attributes` exposes a live `NamedNodeMap` surface with `length`, `item(index)`, `getNamedItem(name)`, `getNamedItemNS(namespace, localName)`, `setNamedItem(...)`, `setNamedItemNS(...)`, `removeNamedItem(...)`, `removeNamedItemNS(...)`, `keys()`, `values()`, `entries()`, and `forEach(callback[, thisArg])`; reflected `id`, `title`, `role`, `slot`, `part`, `ariaLabel`, `ariaDescription`, `ariaRoleDescription`, `ariaHidden`, `lang`, `dir`, `hidden`, `inert`, `translate`, `draggable`, `disabled`, `required`, `noValidate`, `formNoValidate`, `name`, `defaultValue`, `defaultChecked`, `min`, `max`, `step`, `minLength`, `maxLength`, `multiple`, `type`, `placeholder`, `pattern`, `nonce`, `autocapitalize`, `autocomplete`, `autofocus`, `spellcheck`, `inputMode`, `enterKeyHint`, `dirName`, `readOnly`, `tabIndex`, `accessKey`, `contentEditable`, `isContentEditable`, `class`, `checked`, `selected`, and `value` state stays visible through selectors and form-control getters, `HTMLFormElement.autocomplete` reflects the form-level autofill default with `on` / `off` semantics, and `HTMLScriptElement` / `HTMLMetaElement` also expose reflected `src`, `charset`, `text`, `type`, `async`, `defer`, `noModule`, `crossOrigin`, `integrity`, `referrerPolicy`, `fetchPriority` / `name`, `content`, `httpEquiv`, `charset`, `media` metadata on the same phase 8 metadata surfaces; supported form controls also expose a minimal `ValidityState` snapshot via `validity`, including `typeMismatch` and `patternMismatch` for supported text-like inputs. |
| Class and dataset views | Stable Core | 8 | Available | Inline scripts can use `className`, `classList` (including `value`, `item(index)`, `replace()`, `keys()`, `values()`, `entries()`, and `forEach()`), and `dataset` on `Element`; class selectors and `data-*` selectors stay aligned with the shared attribute store. |
| Script tree mutation primitives | Stable Core | 8 | Available | Inline scripts can use `cloneNode()`, `normalize()`, `document.importNode(...)`, `appendChild(...)`, `insertBefore(...)`, `replaceChild(...)`, `removeChild(...)`, `replaceWith(...)`, `replaceChildren(...)`, `append(...)`, `prepend(...)`, `before(...)`, `after(...)`, `insertAdjacentElement(...)`, `insertAdjacentText(...)`, and `remove()` on `Element`; invalid ancestor moves fail explicitly. |
| Script detached construction primitives | Stable Core | 8 | Available | Inline scripts can use `document.createElement()`, `document.createElementNS()`, `document.createAttribute()`, `document.createAttributeNS()`, `document.createTextNode()`, `document.createComment()`, and `document.createDocumentFragment()` to create detached nodes, detached attribute objects, or fragment-like containers that can later be inserted or replaced through the existing mutation helpers; `createElementNS()` is currently limited to the HTML, SVG, and MathML namespaces. |
| Script HTML serialization surfaces | Stable Core | 8 | Available | Inline scripts can use `innerHTML`, `outerHTML`, `insertAdjacentHTML`, `template.content.innerHTML`, and the buffered `document.open()` / `document.write()` / `document.writeln()` / `document.close()` replay slice; getters serialize the implemented subset deterministically with browser-style mixed-quote attribute escaping, setters reuse bounded fragment parsing, basic character reference decoding is applied while parsing HTML text and attributes, position-aware insertion is supported, raw-text contexts stay raw, namespace-aware SVG / MathML name adjustments are preserved, `String(template.content)` yields `[object DocumentFragment]`, and malformed serialization fails explicitly. |
| Inline style declaration surface | Stable Core | 8 | Available | Inline scripts can use `Element.style` as a minimal `CSSStyleDeclaration` surface for semicolon-aware declaration lists with comment stripping and `!important` priority handling, including `cssText`, property reflection, `getPropertyValue(...)`, `getPropertyPriority(...)`, `setProperty(...)`, `removeProperty(...)`, `length`, and `item(index)`; unsupported CSS syntax fails explicitly. |
| Event dispatch and default actions | Stable Core | 3 | Available | `Harness.click()`, `typeText()`, `setChecked()`, `setSelectValue()`, `focus()`, `blur()`, `submit()`, and `dispatch()` now drive capture/bubble listeners, default actions, form-control state, anchor / area navigation/download/target-open/reset observation, details summary activation, popover target activation, print `beforeprint` / `afterprint` lifecycle handlers, and script-side `HTMLElement.click()` / `Element.focus()` / `Element.blur()` focus-state updates; script-side `HTMLAnchorElement.href` / `HTMLAnchorElement.rel` / `HTMLAnchorElement.relList` / `HTMLAnchorElement.download` / `HTMLAnchorElement.target` / `HTMLAnchorElement.ping` / `HTMLAnchorElement.hreflang` / `HTMLAnchorElement.referrerPolicy` / `HTMLAnchorElement.type` and `HTMLAreaElement.href` / `HTMLAreaElement.rel` / `HTMLAreaElement.relList` / `HTMLAreaElement.download` / `HTMLAreaElement.target` / `HTMLAreaElement.ping` / `HTMLAreaElement.alt` / `HTMLAreaElement.coords` / `HTMLAreaElement.shape` / `HTMLAreaElement.noHref` / `HTMLAreaElement.hreflang` / `HTMLAreaElement.referrerPolicy` / `HTMLAreaElement.type` reflection is available for the same link-action slice, `HTMLAnchorElement.text` is exposed as a `textContent` alias on anchors, and the same hyperlink elements also expose read-only URL decomposition properties (`origin`, `protocol`, `host`, `hostname`, `port`, `username`, `password`, `pathname`, `search`, `hash`). |
| Command button activation | Stable Core | 3 | Available | `HTMLButtonElement.command` / `HTMLButtonElement.commandForElement` dispatch `CommandEvent.command` / `CommandEvent.source` through the same deterministic click/default-action path, with target validation limited to the existing dialog and popover helpers; `HTMLButtonElement.type` and `HTMLButtonElement.value` are also reflected as the button's `type` / `value` attributes. |
| Deterministic clock and mock registry | Stable Test Mocks | 4 | Available | `nowMs()`, `advanceTime()`, `flush()`, and `mocksMut()` expose fetch, dialogs, clipboard, open, close, print, scroll, location, matchMedia, downloads, file-input, and storage families with call capture, artifact capture, response injection, failure injection, and reset semantics; print call capture also drives deterministic `beforeprint` / `afterprint` handlers; storage mutations dispatch deterministic `storage` events through `window.addEventListener('storage', ...)` and `window.onstorage`; `advanceTime()` / `flush()` also drive due one-shot and repeating timers, animation frames, plus queued microtasks, `window.performance.now()` follows the fake clock, `matchMedia` keeps `MediaQueryList.matches` aligned with the current seeded state and exposes legacy `addListener()` / `removeListener()` hooks plus `addEventListener('change', ...)` / `removeEventListener('change', ...)` and `onchange`, and scroll call capture is exposed through `ScrollMethod`, `ScrollCall`, and `ScrollMocks`. |
| Hardening suite | Experimental Project | 5 | Available | Contract, regression, and property coverage, plus the publication checklist, are in place for the Zig workspace. |
| Rendering and layout engine | Out of Scope | N/A | Not Planned | This workspace does not aim to become a browser renderer. |
| External network I/O | Out of Scope | N/A | Not Planned | Network behavior is expected to stay mock-driven. |

## Rules

- A capability does not become stable merely because code exists.
- Any new public `Harness` API requires a matrix update before or with the implementation.
- Any new test-only mock must document response injection, failure injection, capture behavior, and reset semantics.
- Planned rows describe the next named milestone and are not user-facing guarantees.
